# requirements.txt (v2 — UPGRADED)
# Core framework
fastapi==0.111.1
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4

# Database
asyncpg==0.29.0

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
passlib==1.7.4

# AI / NLP
anthropic>=0.28.0          # Smart Assistant + AI summaries
rapidfuzz==3.9.3           # Fuzzy matching for reconciliation
numpy==1.26.4

# Optional: enable for full AI embeddings (requires ~400MB RAM)
# sentence-transformers==3.0.1

# File parsing
pdfplumber==0.11.4         # PDF bank statement parsing
openpyxl==3.1.5            # Excel parsing
python-multipart==0.0.9    # File upload support

# Optional: OCR for scanned PDFs (requires tesseract system binary)
# pytesseract==0.3.13
# pdf2image==1.17.0
# Pillow==10.4.0

# Observability
structlog==24.2.0

# Compliance
# (GST + Payroll logic is pure Python, no extra deps)

# Testing
pytest==8.3.2
pytest-asyncio==0.23.8
httpx==0.27.0              # For API integration tests

# Linting (dev only)
# ruff==0.5.0
